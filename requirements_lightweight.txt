# Lightweight Requirements for Deepnote (Memory-Optimized)
# =============================================================
# Memory footprint: ~200MB vs ~2.5GB with local NLP models
#
# What's EXCLUDED (heavy packages):
# ❌ spacy (~200MB)
# ❌ scispacy (~100MB + 400MB models)
# ❌ medspacy (~50MB)
# ❌ sentence-transformers (~500MB)
# ❌ transformers (~800MB if with torch)
#
# What's INCLUDED (lightweight alternatives):
# ✅ API clients for HuggingFace + Claude
# ✅ Database connectivity
# ✅ Data processing (pandas, numpy)
# ✅ Lightweight text processing

# =============================================================================
# Database & Storage
# =============================================================================
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# =============================================================================
# API Clients
# =============================================================================
requests>=2.28.0
httpx>=0.24.0

# Anthropic Claude API (optional, for context analysis)
anthropic>=0.25.0

# =============================================================================
# Data Processing
# =============================================================================
pandas>=2.0.0
numpy>=1.24.0

# =============================================================================
# Text Processing (Lightweight - NO spaCy)
# =============================================================================
# Using simple regex and NLTK instead of heavy spaCy
regex>=2023.0.0

# =============================================================================
# Clustering & ML (Lightweight)
# =============================================================================
scikit-learn>=1.3.0
hdbscan>=0.8.33

# =============================================================================
# Utilities
# =============================================================================
python-dotenv>=1.0.0
pydantic>=2.0.0
